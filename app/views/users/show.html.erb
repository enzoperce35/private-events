<!-- this show page shows that association custom set up is working properly -->

<h4>all_events</h4>
<% @events.each do |event| %>
  <%= event.creator_id %>
  <%= event.creator.email %>
  <%= event.name %>
  <%= event.date %><br>
<% end %>

<hr>

<div>
  <div>
    <h4>my attended_events</h4>
    <% current_user.attended_events.past.each do |past| %>
      <%= past.name %>
      <%= past.date %><br>
    <% end %>
  </div>

  <div>
    <h4>my to be attended future events</h4>
    <% current_user.attended_events.upcoming.each do |up| %>
      <%= up.name %>
      <%= up.date %><br>
    <% end %>
  </div>
</div>